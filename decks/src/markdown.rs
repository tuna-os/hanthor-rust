// markdown.rs — Markdown rendering stub for Decks.
use gtk4::cairo;

pub fn render_markdown(
    _cr: &cairo::Context, _text: &str, _x: f64, _y: f64,
    _max_width: f64, _font_size: f64,
) -> f64 {
    0.0 // stub — full implementation deferred
}
