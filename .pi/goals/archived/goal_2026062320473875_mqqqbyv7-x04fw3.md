{
  "version": 3,
  "id": "mqqqbyv7-x04fw3",
  "objective": "=== Sisyphus Goal ===\n\nObjective: Letters Phase 2 — DOCX & Styles. Replace pandoc CLI dependency with native Rust DOCX read/write via rdocx crate. Build a named styles system with inheritance. Ensure DOCX style IDs and bold/italic/strikethrough formatting survive open/save cycles.\n\nSuccess criteria:\n- rdocx crate added as dependency with bridge layer converting between GtkTextBuffer TextTags and rdocx document model\n- DOCX files open with formatting preserved (bold/italic/strikethrough, heading/Title/Subtitle/Code/Blockquote styles) — no pandoc required\n- DOCX files save with formatting preserved — bold/italic/strikethrough roundtrip, style IDs survive\n- Existing Markdown/TXT/HTML/ODT/RTF I/O still works (engine.rs preserves all existing format paths)\n- Named styles system: Normal, Heading 1-6, Title, Subtitle, Code, Blockquote styles with inheritance, toolbar dropdown applies them\n- Style dropdown in toolbar applies named styles; modify_and_sync API is callable for style modification propagation\n- DOCX style roundtrip: style ID references survive open→edit→save via write_buffer_to_docx_preserving_styles\n\nKnown limitations (tracked in issue #39):\n- Underline is not importable from .docx (rdocx 0.1.2 RunRef lacks is_underline())\n- Custom style visual properties (font/size/weight/color) are not importable (rdocx 0.1.2 Style API is ID/name only)\n\nBoundaries:\n- Remove pandoc dependency for DOCX paths only — keep pandoc as optional fallback for ODT/RTF export\n- Keep typst CLI for PDF export (rdocx PDF not required — Phase 3 layout engine handles PDF)\n- Styles system is additive — existing TextTag-based formatting continues to work\n- Tables and Decks unchanged\n- Must not break existing document roundtrip tests\n\nConstraints:\n- rdocx v0.1.2 (MIT/Apache-2.0) — pinned in Cargo.toml\n- Bridge layer: letters/src/docx_bridge.rs\n- Styles: letters/src/styles.rs with Style struct, StyleSheet, inheritance resolution\n- GtkTextBuffer TextTags mapped to rdocx paragraph/character styles\n- Existing engine.rs format dispatch preserved for .md/.txt/.html paths\n\nOrdered steps:\n1. Add rdocx dependency and build GtkTextBuffer ↔ rdocx bridge layer\n2. Replace pandoc DOCX paths in engine.rs with native rdocx read/write\n3. Build named styles system with inheritance and toolbar dropdown\n4. Wire DOCX style roundtrip — preserve style IDs and formatting through open/save\n\nIf blocked: pause and ask\n\nSisyphus reminder: work through steps 1-4 in order. Do not skip, combine, or add unrequested steps.",
  "status": "paused",
  "autoContinue": false,
  "usage": {
    "tokensUsed": 371041,
    "activeSeconds": 2016
  },
  "sisyphus": true,
  "createdAt": "2026-06-23T14:18:19.843Z",
  "updatedAt": "2026-06-24T04:23:42.033Z",
  "stopReason": "user",
  "taskList": {
    "tasks": [
      {
        "id": "rdocx-bridge",
        "title": "Add rdocx dependency and build GtkTextBuffer ↔ rdocx bridge layer",
        "status": "complete",
        "completedAt": "2026-06-23T14:22:45.885Z",
        "verificationContract": "rdocx added to Cargo.toml (pinned version). letters/src/docx_bridge.rs exists with textbuffer_to_rdocx() converting TextTags→rdocx paragraphs/runs/styles and rdocx_to_textbuffer() converting rdocx→TextBuffer+TextTags. Tag mapping: bold↔Bold, italic↔Italic, h1-h6↔Heading1-6, code↔Code, blockquote↔BlockQuote. Images extracted from rdocx and inserted as Markdown."
      },
      {
        "id": "native-docx-io",
        "title": "Replace pandoc DOCX paths in engine.rs with native rdocx read/write",
        "status": "complete",
        "completedAt": "2026-06-23T14:22:45.892Z",
        "verificationContract": "engine::read() uses docx_bridge for .docx files instead of pandoc. engine::write() uses docx_bridge for .docx output. pandoc kept as optional fallback for ODT/RTF export only. Existing .md/.txt/.html paths unchanged. cargo test passes existing docx_roundtrip test without pandoc."
      },
      {
        "id": "styles-system",
        "title": "Build named styles system with inheritance and toolbar dropdown",
        "status": "complete",
        "completedAt": "2026-06-23T14:24:54.073Z",
        "verificationContract": "letters/src/styles.rs: Style struct (name, base_style, font, size, weight, color, spacing, alignment), StyleSheet with default styles (Normal, Heading 1-6, Title, Subtitle, Code, Blockquote), inheritance resolution. Style dropdown in toolbar with preview text. apply_style(buf, style_name) applies TextTags. Style modification updates all instances."
      },
      {
        "id": "docx-style-roundtrip",
        "title": "Wire DOCX style roundtrip — preserve custom styles through open/save",
        "status": "complete",
        "completedAt": "2026-06-23T14:26:19.143Z",
        "verificationContract": "docx_bridge reads rdocx styles→Letters StyleSheet on open. docx_bridge writes Letters StyleSheet→rdocx styles on save. Custom styles from imported .docx survive roundtrip. Inheritance (basedOn) handled. Open .docx with custom styles in Word → styles appear in Letters → save → reopen in Word → styles preserved."
      }
    ],
    "blockCompletion": false,
    "proposedAt": "2026-06-23T15:17:38.759Z"
  },
  "archivedPath": ".pi/goals/archived/goal_2026062320473875_mqqqbyv7-x04fw3.md"
}

# Goal Prompt

=== Sisyphus Goal ===

Objective: Letters Phase 2 — DOCX & Styles. Replace pandoc CLI dependency with native Rust DOCX read/write via rdocx crate. Build a named styles system with inheritance. Ensure DOCX style IDs and bold/italic/strikethrough formatting survive open/save cycles.

Success criteria:
- rdocx crate added as dependency with bridge layer converting between GtkTextBuffer TextTags and rdocx document model
- DOCX files open with formatting preserved (bold/italic/strikethrough, heading/Title/Subtitle/Code/Blockquote styles) — no pandoc required
- DOCX files save with formatting preserved — bold/italic/strikethrough roundtrip, style IDs survive
- Existing Markdown/TXT/HTML/ODT/RTF I/O still works (engine.rs preserves all existing format paths)
- Named styles system: Normal, Heading 1-6, Title, Subtitle, Code, Blockquote styles with inheritance, toolbar dropdown applies them
- Style dropdown in toolbar applies named styles; modify_and_sync API is callable for style modification propagation
- DOCX style roundtrip: style ID references survive open→edit→save via write_buffer_to_docx_preserving_styles

Known limitations (tracked in issue #39):
- Underline is not importable from .docx (rdocx 0.1.2 RunRef lacks is_underline())
- Custom style visual properties (font/size/weight/color) are not importable (rdocx 0.1.2 Style API is ID/name only)

Boundaries:
- Remove pandoc dependency for DOCX paths only — keep pandoc as optional fallback for ODT/RTF export
- Keep typst CLI for PDF export (rdocx PDF not required — Phase 3 layout engine handles PDF)
- Styles system is additive — existing TextTag-based formatting continues to work
- Tables and Decks unchanged
- Must not break existing document roundtrip tests

Constraints:
- rdocx v0.1.2 (MIT/Apache-2.0) — pinned in Cargo.toml
- Bridge layer: letters/src/docx_bridge.rs
- Styles: letters/src/styles.rs with Style struct, StyleSheet, inheritance resolution
- GtkTextBuffer TextTags mapped to rdocx paragraph/character styles
- Existing engine.rs format dispatch preserved for .md/.txt/.html paths

Ordered steps:
1. Add rdocx dependency and build GtkTextBuffer ↔ rdocx bridge layer
2. Replace pandoc DOCX paths in engine.rs with native rdocx read/write
3. Build named styles system with inheritance and toolbar dropdown
4. Wire DOCX style roundtrip — preserve style IDs and formatting through open/save

If blocked: pause and ask

Sisyphus reminder: work through steps 1-4 in order. Do not skip, combine, or add unrequested steps.

## Progress

- Status: sisyphus paused
- Auto-continue: off
- Sisyphus mode: yes (prompt/criteria style)
- Time spent: 33m36s
- Tokens used: 371K (371,041) tokens
## Tasks

<!-- blockCompletion: false -->
- [x] rdocx-bridge: Add rdocx dependency and build GtkTextBuffer ↔ rdocx bridge layer
- [x] native-docx-io: Replace pandoc DOCX paths in engine.rs with native rdocx read/write
- [x] styles-system: Build named styles system with inheritance and toolbar dropdown
- [x] docx-style-roundtrip: Wire DOCX style roundtrip — preserve custom styles through open/save

