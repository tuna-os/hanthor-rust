{
  "version": 3,
  "id": "mqqw2n3v-ho4w1q",
  "objective": "=== Sisyphus Goal ===\n\nObjective: Letters Phase 3 — Print Layout. Make Letters paginate documents into pages with visual page breaks, headers/footers, and print preview. Integrate a layout engine to calculate page boundaries. No columns or zoom yet (Phase 4).\n\nSuccess criteria:\n- Layout engine paginates the document text into pages based on GSettings page size and margins\n- Visual page breaks appear in the editor as gaps or separator lines between pages\n- Headers and footers are editable per-section with page numbering support\n- Print preview renders the paginated document as pages matching print output\n- DOCX roundtrip preserves page breaks, headers/footers, and section properties\n- Existing editing features (formatting, styles, spell-check) work normally with paginated documents\n\nBoundaries:\n- No multi-column layout (Phase 4)\n- No zoom (Phase 4)\n- No line spacing or tab stop controls (Phase 4)\n- Keep GtkTextView for editing — pagination is a layout layer on top, not a rewrite\n- Tables and Decks unchanged\n- Must not break existing tests or editing features\n\nConstraints:\n- Layout engine: use PangoLayout text measurement for pagination (no new Rust crate dependency). Pango is already available via pangocairo.\n- PageContainer widget extended to render multiple pages, not just one\n- Headers/footers are separate GtkTextViews within each page container\n- Print preview reuses the same Cairo rendering as the editor view\n- GSettings used for page size, margins, header/footer distances\n\nOrdered steps:\n1. Build layout engine — paginate buffer text into pages using PangoLayout measurement, calculate page breaks\n2. Render page breaks — show visual gaps or separator lines between pages in the editor\n3. Build headers/footers — editable per-section areas with page number field\n4. Build print preview — render paginated document as a preview dialog or overlay\n5. Wire DOCX layout roundtrip — preserve page breaks, headers/footers, section properties through rdocx\n\nIf blocked: pause and ask\n\nSisyphus reminder: work through steps 1-5 in order. Do not skip, combine, or add unrequested steps.",
  "status": "active",
  "autoContinue": true,
  "usage": {
    "tokensUsed": 1123891,
    "activeSeconds": 370
  },
  "sisyphus": true,
  "createdAt": "2026-06-23T16:59:02.395Z",
  "updatedAt": "2026-06-23T17:05:34.126Z",
  "activePath": ".pi/goals/active_goal_2026062322290239_mqqw2n3v-ho4w1q.md",
  "taskList": {
    "tasks": [
      {
        "id": "layout-engine",
        "title": "Build layout engine — paginate buffer text into pages via PangoLayout",
        "status": "complete",
        "completedAt": "2026-06-23T17:00:20.195Z",
        "verificationContract": "layout.rs: paginate() function takes GtkTextBuffer + page dimensions + margins, returns Vec<Page> with start/end TextIter positions. Runs async or debounced (300ms after last edit). Handles 50-page documents in <100ms. Uses pangocairo::functions::create_layout() for text measurement."
      },
      {
        "id": "page-breaks",
        "title": "Render visual page breaks between pages in the editor",
        "status": "complete",
        "completedAt": "2026-06-23T17:04:11.185Z",
        "verificationContract": "Page break visualization: gaps or dashed separator lines between pages in the PageContainer widget. Page numbers displayed at break positions. Page breaks update when layout engine recalculates after edits. Editor scrolls through pages continuously. GtkTextView editing works across page boundaries."
      },
      {
        "id": "headers-footers",
        "title": "Build headers/footers — editable per-section areas with page numbers",
        "status": "complete",
        "completedAt": "2026-06-23T17:05:34.121Z",
        "verificationContract": "Each page has a header area (top) and footer area (bottom) rendered as small GtkTextViews. Double-click to edit. Page number field {page} resolves to current page number. Different first page header/footer supported. Header/footer content stored per-section."
      },
      {
        "id": "print-preview",
        "title": "Build print preview — render paginated document as it will print",
        "status": "pending",
        "verificationContract": "Print preview window or overlay showing paginated document with correct page sizing, headers, footers, and page numbers. Uses same layout engine as the editor. Matches what GtkPrintOperation would produce. Page navigation (prev/next)."
      },
      {
        "id": "docx-layout-roundtrip",
        "title": "Wire DOCX layout roundtrip — preserve page breaks, headers/footers, sections",
        "status": "pending",
        "verificationContract": "Open .docx with page breaks, headers, footers — they appear in Letters. Save .docx — page breaks, headers, footers, section properties preserved. Roundtrip verified by opening a Word document, editing in Letters, saving, reopening in Word."
      }
    ],
    "blockCompletion": false,
    "proposedAt": "2026-06-23T16:59:02.403Z"
  }
}

# Goal Prompt

=== Sisyphus Goal ===

Objective: Letters Phase 3 — Print Layout. Make Letters paginate documents into pages with visual page breaks, headers/footers, and print preview. Integrate a layout engine to calculate page boundaries. No columns or zoom yet (Phase 4).

Success criteria:
- Layout engine paginates the document text into pages based on GSettings page size and margins
- Visual page breaks appear in the editor as gaps or separator lines between pages
- Headers and footers are editable per-section with page numbering support
- Print preview renders the paginated document as pages matching print output
- DOCX roundtrip preserves page breaks, headers/footers, and section properties
- Existing editing features (formatting, styles, spell-check) work normally with paginated documents

Boundaries:
- No multi-column layout (Phase 4)
- No zoom (Phase 4)
- No line spacing or tab stop controls (Phase 4)
- Keep GtkTextView for editing — pagination is a layout layer on top, not a rewrite
- Tables and Decks unchanged
- Must not break existing tests or editing features

Constraints:
- Layout engine: use PangoLayout text measurement for pagination (no new Rust crate dependency). Pango is already available via pangocairo.
- PageContainer widget extended to render multiple pages, not just one
- Headers/footers are separate GtkTextViews within each page container
- Print preview reuses the same Cairo rendering as the editor view
- GSettings used for page size, margins, header/footer distances

Ordered steps:
1. Build layout engine — paginate buffer text into pages using PangoLayout measurement, calculate page breaks
2. Render page breaks — show visual gaps or separator lines between pages in the editor
3. Build headers/footers — editable per-section areas with page number field
4. Build print preview — render paginated document as a preview dialog or overlay
5. Wire DOCX layout roundtrip — preserve page breaks, headers/footers, section properties through rdocx

If blocked: pause and ask

Sisyphus reminder: work through steps 1-5 in order. Do not skip, combine, or add unrequested steps.

## Progress

- Status: sisyphus running
- Auto-continue: on
- Sisyphus mode: yes (prompt/criteria style)
- Time spent: 6m10s
- Tokens used: 1.1M (1,123,891) tokens
## Tasks

<!-- blockCompletion: false -->
- [x] layout-engine: Build layout engine — paginate buffer text into pages via PangoLayout
- [x] page-breaks: Render visual page breaks between pages in the editor
- [x] headers-footers: Build headers/footers — editable per-section areas with page numbers
- [ ] print-preview: Build print preview — render paginated document as it will print — contract: Print preview window or overlay showing paginated document with correct page sizing, headers, footers, and page numbers. Uses same layout engine as the editor. Matches what GtkPrintOperation would produce. Page navigation (prev/next).
- [ ] docx-layout-roundtrip: Wire DOCX layout roundtrip — preserve page breaks, headers/footers, sections — contract: Open .docx with page breaks, headers, footers — they appear in Letters. Save .docx — page breaks, headers, footers, section properties preserved. Roundtrip verified by opening a Word document, editing in Letters, saving, reopening in Word.

